// Огры взяли в заложники нескольких крестьян!
// Ваши разведчики дали вам разведданные необходимые для засады.
// this.grid содержит массив массивов
// Внутри этих подмассивов, 0 - это крестьянин и 1 - это огр!
// Используйте эту информацию для установки огненных ловушек, чтобы одолеть конвой.

// Во-первых, помните содержащий массив это просто массив!
// Переберите все элементы этого массива
for(var i = 0; i < hero.grid.length; i++) {
    var row = hero.grid[i];
    // Теперь, строка это просто еще один массив!
    // Переберите все ячейки в этом массиве
    for(var j = 0; j < row.length; j++){
        // Выполните проверку, чтобы увидеть что ячейка j строки i равна 1
        if (row[j] === 1) {    
        hero.buildXY("fire-trap", 36 + 6 * j, 20 + 6 * i);
        }
    }
}
// И наконец, отступите в укрытие
hero.moveXY(22, 44);
hero.moveXY(29, 55);
